name = "Nestor"
last_name = "Schmith"

# Concatenación de nombres
completed_name = name + " " + last_name
print(completed_name)

# Formateo de texto
formated_name = f"{name} {last_name}"
print(formated_name)

# El formateo permite realizar cambios a las variables
changed_name = f"{name.upper()} {last_name.lower()}"
print(changed_name)